# Local CI stack settings. Copy to .env.ci and set your own values.
# Real secrets never belong in the repository - .gitignore excludes .env.ci.

JENKINS_ADMIN_ID=admin
JENKINS_ADMIN_PASSWORD=change_me_before_first_start
SONAR_DB_PASSWORD=change_me_too

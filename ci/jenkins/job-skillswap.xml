<?xml version='1.1' encoding='UTF-8'?>
<!--
  The SkillSwap pipeline job. Posted to Jenkins by ci/jenkins/create-job.sh.

  The repository URL is a placeholder that the script substitutes, so the same
  file serves the local working copy at Stage A and the GitHub remote later.
-->
<flow-definition plugin="workflow-job">
  <description>SkillSwap CI/CD - checkout, test, SonarQube, Docker build. Defined in the repository's Jenkinsfile.</description>
  <keepDependencies>false</keepDependencies>
  <properties>
    <jenkins.model.BuildDiscarderProperty>
      <strategy class="hudson.tasks.LogRotator">
        <daysToKeep>-1</daysToKeep>
        <numToKeep>20</numToKeep>
        <artifactDaysToKeep>-1</artifactDaysToKeep>
        <artifactNumToKeep>5</artifactNumToKeep>
      </strategy>
    </jenkins.model.BuildDiscarderProperty>
  </properties>
  <definition class="org.jenkinsci.plugins.workflow.cps.CpsScmFlowDefinition" plugin="workflow-cps">
    <scm class="hudson.plugins.git.GitSCM" plugin="git">
      <configVersion>2</configVersion>
      <userRemoteConfigs>
        <hudson.plugins.git.UserRemoteConfig>
          <url>@REPO_URL@</url>
        </hudson.plugins.git.UserRemoteConfig>
      </userRemoteConfigs>
      <branches>
        <hudson.plugins.git.BranchSpec>
          <name>*/main</name>
        </hudson.plugins.git.BranchSpec>
      </branches>
      <doGenerateSubmoduleConfigurations>false</doGenerateSubmoduleConfigurations>
      <extensions/>
    </scm>
    <scriptPath>Jenkinsfile</scriptPath>
    <lightweight>false</lightweight>
  </definition>
  <triggers>
    <!--
      Poll GitHub every five minutes and build when the commit changes.

      Polling rather than a webhook because this Jenkins is on localhost and
      GitHub cannot reach it. If the instance is ever exposed, replace this
      with a GitHub webhook: it is instant and costs no requests.

      H spreads the poll across the interval instead of firing on the minute,
      so several jobs do not stampede together.
    -->
    <hudson.triggers.SCMTrigger>
      <spec>H/5 * * * *</spec>
      <ignorePostCommitHooks>false</ignorePostCommitHooks>
    </hudson.triggers.SCMTrigger>
  </triggers>
  <disabled>false</disabled>
</flow-definition>
